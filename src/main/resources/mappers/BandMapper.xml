<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="mappers.BandMapper">

    <insert id="insertOne" parameterType="Band" useGeneratedKeys="true" keyProperty="no">
        INSERT INTO band (memberNo, id, name, nickname, description)
        VALUES (#{memberNo}, #{id}, #{name}, #{nickname}, #{description})
    </insert>
    <!-- 전체 밴드 조회 -->
    <select id="selectAll" resultType="Band">
        SELECT *
        FROM band
        ORDER BY createdAt DESC
    </select>
    <!-- 전체 밴드 조회 -->
    <select id="selectAllJoined" resultType="Band">
        SELECT *
        FROM band where no in
        (select bandNo from bandMember where id=#{id})
        ORDER BY createdAt DESC
    </select>

    <!-- 밴드 번호로 특정 밴드 조회 -->
    <select id="selectByNo" parameterType="int" resultType="Band">
        SELECT *
        FROM band
        WHERE no = #{no}
    </select>

    <!-- 키워드 검색어 찾기 -->
    <select id="selectLikeKeywordByOffset"
            resultType="band" parameterType="map">
        select * from band
        where title like #{keyword} or content like #{keyword}
        order by wroteAt desc limit 10 offset #{offset}
    </select>

    <!-- 좋아요수 증가 -->
    <update id="increaseLikeCnt" parameterType="int">
        UPDATE band SET likeCnt = likeCnt + 1 WHERE no = #{no}
    </update>
    <!-- 좋아요수 감소 -->
    <update id="decreaseLikeCnt" parameterType="int">
        update band set likeCnt=likeCnt- 1 where no=#{no}
    </update>
    <!-- 좋아요수 상위 5개 밴드 -->
    <select id="selectTop5Likes" resultType="Band">
        SELECT * FROM band
        ORDER BY likeCnt DESC
        LIMIT 5
    </select>

    <!-- 조회수 증가 -->
    <update id="increaseViewCnt" parameterType="int">
        UPDATE band SET viewCnt = viewCnt + 1 WHERE no = #{no}
    </update>
    <!-- 조회수 상위 5개 밴드 -->
    <select id="selectTop5Views" resultType="Band">
        SELECT * FROM band
        ORDER BY viewCnt DESC
        LIMIT 5
    </select>


    <!-- 밴드 수정 -->
    <update id="updateOne" parameterType="band">
        update band
        set name=#{name}, description=#{description}
        where no=#{no}
    </update>

    <!-- 밴드 삭제 -->
    <delete id="deleteByNo" parameterType="int">
        delete from band where no=#{no}
    </delete>




</mapper>
